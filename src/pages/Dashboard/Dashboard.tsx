import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div style={{ padding: '2rem', color: 'var(--text-primary)' }}>
            <h1>Dashboard</h1>
            <p>Welcome to TeacherCalc Main Page.</p>
        </div>
    );
};

export default Dashboard;
